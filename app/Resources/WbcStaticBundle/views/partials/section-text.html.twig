<section class="section-text bg-grey">
    <div class="container">
        <div class="row">
            <div class="col-md-12 text-center">
                <h2>{{ title }}</h2>

                <div class="row">
                    <div class="col-md-8 col-md-offset-2">
                        {{ content|raw }}
                    </div>
                </div>
                {% if buttonText is defined %}
                    <div class="row">
                        <div class="col-md-4 col-md-offset-4">
                            <a href="{{ path('wbc_valuation_index') }}" class="btn btn-primary btn-lg">{{ buttonText }}</a>
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</section>