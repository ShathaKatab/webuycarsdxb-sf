<valuation-step-one form-action-url="'{{ path('wbc_valuation_index') }}'"
                    form-class="form-inline"
                    form-group-class="{{ groupClass|default('col-md-3 col-xs-12') }}"
                    form-button-text="{{ buttonText|default('Get FREE Online Valuation') }}"
                    vehicle-makes="{{ getMakes()|serialize }}"
                    vehicle-years="{{ getModelYears()|serialize }}">
</valuation-step-one>